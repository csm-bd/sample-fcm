# 🔥 Flutter FCM Integration Guide
This project demonstrates how to configure push notifications for both Android and iOS devices, including testing notifications using Postman.
## 🚀 Step 1: Create Flutter Project
## 📦 Step 2: Change Application Package Name
### 🔄 Change Application Package Name
  - install using this command on your terminal
  ```bash flutter pub add change_app_package_name ```
  - Now Run this command to change the package name for both platforms.
  ```bash dart run change_app_package_name:main com.your_package_name ```